# Scratch workspace for the coding agent (M3+).
# All read/write/edit/glob/grep tools are jailed under this directory
# (or WORKSPACE_ROOT if set). Do not point WORKSPACE_ROOT at sensitive paths.
#
# Plugins (M16/M23): `plugins/<id>/plugin.yaml` (+ optional skills/mcp/subagents).
# Example packs seed on first agent/plugin resolve: review, docs-mcp, research.
